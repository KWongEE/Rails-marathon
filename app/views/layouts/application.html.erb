<!DOCTYPE html>
<html>
<head>
  <title>Fantasy Houses</title>
  <%= stylesheet_link_tag    'application', media: 'all' %>
  <%= javascript_include_tag 'application' %>
  <%= csrf_meta_tags %>
</head>
<body>

<!-- PUT FLASH HERE -->

  <% if flash.any? %>
  <div class="flash">
    <% flash.each do |name, msg| %>
      <%= content_tag :div, msg, class: "callout #{name}" %>
    <% end %>
  </div>
<% end %>

<%= yield %>

</body>
</html>
