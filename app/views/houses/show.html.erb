<%= @house.name %>
<%= @house.author %>
<%= @house.source %>
<%= @house.motto %>

<ul>
  <% @members.each do |member| %>
    <%= member.first_name %>
    <%= member.last_name %>
    <%= member.created_at.strftime("%B %d %Y") %>
    <% end %>
</ul>

<%= link_to 'Home',root_path %>
<%= link_to 'Add a House Member', new_house_member_path(@house) %>
